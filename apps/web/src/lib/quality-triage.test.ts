import{describe,expect,test}from"bun:test";import{suggestQualityClassification,validateQualityUpdate}from"./quality-triage";describe("quality triage",()=>{test("suggests safety and spoilage categories",()=>{expect(suggestQualityClassification("Found hair in food").category).toBe("HYGIENE_SAFETY");expect(suggestQualityClassification("meal was spoiled").category).toBe("FRESHNESS_SPOILAGE")});test("rejects unreviewable values",()=>expect(validateQualityUpdate({id:"1",category:"DELETE",severity:"HIGH",status:"NEW"})).toBeNull())});
