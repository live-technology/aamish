import { redirect } from "next/navigation";import { currentSession } from "@/lib/auth";import { db } from "@/lib/db";import { QualityDashboard,type ReviewRow } from "@/components/quality-dashboard";
export default async function QualityPage(){const session=await currentSession();if(!session)redirect("/login");if(session.role!=="SUPER_ADMIN")redirect("/");const reviews=await db()<ReviewRow[]>`SELECT mr.id,mr.rating,mr.comment,ep.full_name,e.name AS enterprise_name,COALESCE(m.title,'Scheduled meal') AS menu_title,mr.created_at::text,ms.schedule_date::text,COUNT(rp.id)::int AS photo_count FROM meal_reviews mr JOIN menu_schedules ms ON ms.id=mr.schedule_id JOIN employees ep ON ep.id=mr.employee_id JOIN enterprises e ON e.id=ep.enterprise_id LEFT JOIN meal_preferences mp ON mp.schedule_id=mr.schedule_id AND mp.employee_id=mr.employee_id LEFT JOIN menu_schedule_options mso ON mso.id=mp.selected_option_id LEFT JOIN menus m ON m.id=mso.menu_id LEFT JOIN review_photos rp ON rp.review_id=mr.id WHERE ms.schedule_date>=CURRENT_DATE-29 GROUP BY mr.id,ms.schedule_date,ep.full_name,e.name,m.title ORDER BY ms.schedule_date DESC,mr.created_at DESC`;return <QualityDashboard reviews={[...reviews]}/>}
