import { redirect } from "next/navigation";import { currentSession } from "@/lib/auth";import { db } from "@/lib/db";import { OperationsDashboard,type OperationRow } from "@/components/operations-dashboard";
export default async function OperationsPage(){const session=await currentSession();if(!session)redirect("/login");if(session.role!=="SUPER_ADMIN")redirect("/");const rows=await db()<OperationRow[]>`SELECT ms.schedule_date::text,e.name AS enterprise_name,dl.name AS location_name,mso.option_label,m.title AS menu_title,COUNT(mp.id)::int AS meal_count FROM menu_schedules ms JOIN enterprises e ON e.id=ms.enterprise_id JOIN meal_preferences mp ON mp.schedule_id=ms.id AND mp.is_opted_in=TRUE JOIN delivery_locations dl ON dl.id=mp.location_id JOIN menu_schedule_options mso ON mso.id=mp.selected_option_id JOIN menus m ON m.id=mso.menu_id WHERE ms.schedule_date BETWEEN CURRENT_DATE-7 AND CURRENT_DATE+14 GROUP BY ms.schedule_date,e.name,dl.name,mso.option_label,m.title ORDER BY ms.schedule_date,dl.name,mso.option_label`;return <OperationsDashboard rows={[...rows]}/>}
