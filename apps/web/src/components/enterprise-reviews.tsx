"use client";
import { Mic, Search, Star } from "lucide-react";
import { useMemo, useState } from "react";
import { AppShell } from "@/components/ui/app-shell";
import { EmptyState, PageHeader, StatusBadge } from "@/components/ui/primitives";
import { enterpriseNavigation } from "@/lib/enterprise-navigation";
import styles from "./enterprise-reviews.module.css";

export type EnterpriseReviewRow = { id:string; rating:number; comment:string|null; created_at:string; schedule_date:string; full_name:string; location_name:string; menu_title:string; voice_url?:string|null; voice_duration_seconds?:number|null };
export function filterEnterpriseReviews(rows:EnterpriseReviewRow[],filters:{query:string;location:string;date:string;rating:string}){const q=filters.query.trim().toLowerCase();return rows.filter(row=>(!q||`${row.full_name} ${row.comment||""} ${row.menu_title} ${row.location_name}`.toLowerCase().includes(q))&&(!filters.location||row.location_name===filters.location)&&(!filters.date||row.schedule_date===filters.date)&&(!filters.rating||(filters.rating==="LOW"?row.rating<=2:String(row.rating)===filters.rating)));}
export function EnterpriseReviews({enterpriseName,fullName,reviews}:{enterpriseName:string;fullName:string;reviews:EnterpriseReviewRow[]}){const[query,setQuery]=useState("");const[location,setLocation]=useState("");const[date,setDate]=useState("");const[rating,setRating]=useState("");const filtered=useMemo(()=>filterEnterpriseReviews(reviews,{query,location,date,rating}),[reviews,query,location,date,rating]);const locations=[...new Set(reviews.map(row=>row.location_name))].sort();const average=reviews.length?reviews.reduce((sum,row)=>sum+row.rating,0)/reviews.length:null;return <AppShell workspace={enterpriseName} fullName={fullName} roleLabel="Enterprise administrator" currentPath="/enterprise/reviews" navigation={enterpriseNavigation}><PageHeader eyebrow="Reviews" title="Employee meal feedback" description="Understand the last 30 days of meal ratings, comments, photos, and voice reviews without mixing them with product feedback."/><section className={styles.summary}><article><strong>{reviews.length}</strong><span>Reviews</span></article><article><strong>{average?.toFixed(1)||"—"}</strong><span>Average rating</span></article><article><strong>{new Set(reviews.map(row=>row.schedule_date)).size}</strong><span>Meal days</span></article><article><strong>{reviews.filter(row=>row.rating<=2).length}</strong><span>Low ratings</span></article></section>{reviews.length>0&&<div className={styles.filters}><label className={styles.search}><Search size={15}/><span className="sr-only">Search reviews</span><input placeholder="Search employee, menu or comment" value={query} onChange={e=>setQuery(e.target.value)}/></label><select aria-label="Location" value={location} onChange={e=>setLocation(e.target.value)}><option value="">All locations</option>{locations.map(item=><option key={item}>{item}</option>)}</select><input aria-label="Meal date" type="date" value={date} onChange={e=>setDate(e.target.value)}/><select aria-label="Rating" value={rating} onChange={e=>setRating(e.target.value)}><option value="">All ratings</option><option value="LOW">Low (1–2)</option>{[5,4,3,2,1].map(value=><option value={value} key={value}>{value} stars</option>)}</select></div>}{reviews.length===0?<EmptyState icon={<Star size={25}/>} title="No reviews yet" description="Employee meal reviews will appear here after submission."/>:filtered.length===0?<EmptyState icon={<Search size={25}/>} title="No reviews match" description="Clear a filter or choose another date, rating, or location."/>:<section className={styles.list}>{filtered.map(review=><article className={styles.review} key={review.id}><header><div><strong>{review.full_name}</strong><span>{review.menu_title} · {review.location_name}</span></div><StatusBadge tone={review.rating<=2?"dangerTone":review.rating>=4?"success":"neutral"}><Star size={12} fill="currentColor"/>{review.rating}/5</StatusBadge></header>{review.comment?<p>{review.comment}</p>:<p className={styles.muted}>No written comment.</p>}{review.voice_url&&<div className={styles.voice}><Mic size={15}/><audio controls preload="none" src={review.voice_url}/><span>{review.voice_duration_seconds}s</span></div>}<footer><time dateTime={review.schedule_date}>Meal {formatDate(review.schedule_date)}</time><time dateTime={review.created_at}>Submitted {formatTime(review.created_at)}</time></footer></article>)}</section>}</AppShell>}
function formatDate(value:string){return new Date(`${value}T00:00:00`).toLocaleDateString("en-BD",{day:"numeric",month:"short",year:"numeric"})}function formatTime(value:string){return new Date(value).toLocaleString("en-BD",{timeZone:"Asia/Dhaka",dateStyle:"medium",timeStyle:"short"})}
