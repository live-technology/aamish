"use client";

import Image from "next/image";
import { FileUp, LogOut, Plus, Users, X } from "lucide-react";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";

type Location={id:string;name:string;code:string}; export type Employee={id:string;employee_code:string;full_name:string;email:string;location_name:string;username:string;is_active:boolean};

export function EnterprisePortal({enterpriseName,fullName,locations,initialEmployees}:{enterpriseName:string;fullName:string;locations:Location[];initialEmployees:Employee[]}){
 const router=useRouter(); const[employees,setEmployees]=useState(initialEmployees); const[open,setOpen]=useState(false); const[saving,setSaving]=useState(false); const[message,setMessage]=useState("");
 async function reload(){const r=await fetch("/api/enterprise/employees");const d=await r.json();if(r.ok)setEmployees(d.employees)}
 async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setMessage("");const form=new FormData(event.currentTarget);const response=await fetch("/api/enterprise/employees",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const data=await response.json();setSaving(false);if(!response.ok)return setMessage(data.error);setOpen(false);await reload()}
 function downloadTemplate(){const csv=`employeeCode,fullName,email,locationCode,username,password\nEMP-001,Employee Name,employee@company.com,${locations[0]?.code||"LOCATION-CODE"},employee.001,welcome123\n`;const url=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));const link=document.createElement("a");link.href=url;link.download=`${enterpriseName.toLowerCase().replaceAll(" ","-")}-employees.csv`;link.click();URL.revokeObjectURL(url)}
 async function uploadCsv(file:File){const lines=(await file.text()).split(/\r?\n/).filter(Boolean);const headers=lines.shift()?.split(",").map(v=>v.trim())||[];const rows=lines.map(line=>Object.fromEntries(line.split(",").map((value,index)=>[headers[index],value.trim()])));const response=await fetch("/api/enterprise/employees/bulk",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({employees:rows})});const data=await response.json();setMessage(response.ok?`${data.inserted} employees added; ${data.errors.length} rows failed.`:data.error);await reload()}
 return <div className="admin-portal"><aside className="admin-sidebar"><Image src="/brand/amish-logo-01.png" alt="Aamish" width={130} height={44}/><p>{enterpriseName}</p><nav><a className="active"><Users size={17}/> Employees</a></nav><button onClick={()=>fetch("/api/auth/logout",{method:"POST"}).then(()=>router.push("/login"))}><LogOut size={16}/> Sign out</button></aside><main className="admin-main"><header className="admin-header"><div><p className="eyebrow">ENTERPRISE ADMIN</p><h1>Employee roster</h1><span>Welcome, {fullName}. Manage employees and their delivery locations.</span></div><button className="primary" onClick={()=>setOpen(true)}><Plus size={17}/> Add employee</button></header><div className="roster-actions"><button onClick={downloadTemplate}>Download CSV template</button><label><FileUp size={16}/> Upload completed CSV<input type="file" accept=".csv,text/csv" onChange={e=>e.target.files?.[0]&&void uploadCsv(e.target.files[0])}/></label></div>{message&&<p className="action-message">{message}</p>}{employees.length===0?<section className="empty-dashboard"><div className="empty-icon"><Users size={28}/></div><h2>No employees yet</h2><p>Add one employee or upload the location-aware CSV template.</p><button className="primary" onClick={()=>setOpen(true)}><Plus size={17}/> Add first employee</button></section>:<section className="employee-table"><div className="employee-row employee-head"><span>Employee</span><span>ID / Username</span><span>Location</span><span>Status</span></div>{employees.map(employee=><div className="employee-row" key={employee.id}><div><b>{employee.full_name}</b><small>{employee.email}</small></div><div><b>{employee.employee_code}</b><small>{employee.username}</small></div><span>{employee.location_name}</span><em>{employee.is_active?"ACTIVE":"INACTIVE"}</em></div>)}</section>}</main>{open&&<div className="form-overlay"><form className="enterprise-form" onSubmit={submit}><header><div><p className="eyebrow">NEW EMPLOYEE</p><h2>Add employee</h2><span>Fields marked <b className="required-mark">*</b> are required.</span></div><button type="button" onClick={()=>setOpen(false)}><X size={20}/></button></header><section><div className="field-grid"><label className="required">Full name<input name="fullName" required/></label><label className="required">Employee ID<input name="employeeCode" required/></label><label className="required">Corporate email<input name="email" type="email" required/></label><label>Phone <small>Optional</small><input name="phone"/></label><label className="wide required">Delivery location<select name="locationId" required defaultValue=""><option value="" disabled>Select location</option>{locations.map(location=><option value={location.id} key={location.id}>{location.name} ({location.code})</option>)}</select></label><label className="required">Username<input name="username" required/></label><label className="required">Temporary password<input name="password" type="password" required/></label></div></section>{message&&<p className="form-error form-error-inline">{message}</p>}<footer><button type="button" className="cancel-button" onClick={()=>setOpen(false)}>Cancel</button><button className="primary" disabled={saving}>{saving?"Creating…":"Create employee"}</button></footer></form></div>}</div>
}
