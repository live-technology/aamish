import { AdminNav } from "@/components/admin-nav";import { Star } from "lucide-react";
export type ReviewRow={id:string;rating:number;comment:string|null;full_name:string;enterprise_name:string;menu_title:string;created_at:string;photo_count:number};
export function QualityDashboard({reviews}:{reviews:ReviewRow[]}){const average=reviews.length?reviews.reduce((sum,row)=>sum+row.rating,0)/reviews.length:0;return <div className="admin-portal"><AdminNav active="quality"/><main className="admin-main"><header className="admin-header"><div><p className="eyebrow">QUALITY PULSE</p><h1>Reviews & CSAT</h1><span>Employee feedback across enterprises and meal packages.</span></div></header><div className="enterprise-summary"><article><strong>{average.toFixed(1)}</strong><span>Average CSAT</span></article><article><strong>{reviews.length}</strong><span>Total reviews</span></article><article><strong>{reviews.filter(review=>review.rating<=3).length}</strong><span>Flagged reviews</span></article></div>{reviews.length===0?<section className="empty-dashboard"><h2>No reviews yet</h2><p>Submitted employee feedback will appear here.</p></section>:<section className="review-list">{reviews.map(review=><article className={review.rating<=3?"quality-review flagged":"quality-review"} key={review.id}><div><b>{review.full_name}</b><span>{review.enterprise_name} · {review.menu_title}</span></div><strong>{review.rating}<Star size={14} fill="currentColor"/></strong><p>{review.comment||"No written comment."}</p><small>{review.photo_count} photos</small></article>)}</section>}</main></div>}
