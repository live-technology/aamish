# AAM-064 — Ascending feedback ratings

Request: display ratings left to right from 1 to 5 on both feedback forms.

Acceptance: 😞 1, 😕 2, 🙂 3, 😋 4, 🤩 5. Numeric values and emoji mappings remain intact. Both forms use the shared control.

Out of scope: all other form behavior, branding, storage and public URLs.

Validation: required test/lint/build commands once, plus a focused responsive check of the shared rating control. No database migration or environment changes.
