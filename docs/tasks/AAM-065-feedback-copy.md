# AAM-065 — Simplify feedback form copy

Request: replace the shared feedback headline with “Tell us how we did, we’re listening”; remove “Optional” from public review/voice helper text, hide name/phone asterisks, remove the visibility footer, and use the requested thank-you message.

Acceptance: exact revised copy, no duplicate heading/subtitle, no name/phone asterisks; name/phone validation and anonymous clearing remain unchanged. Ratings stay ascending. The shared heading updates employee and public forms.

Out of scope: submission behavior, branding, access controls, database, public URLs.

Validation: required tests/lint/build once, plus focused Docker checks of changed copy and required identity behavior at desktop/mobile widths. No migration or environment changes.
